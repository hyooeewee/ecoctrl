# Backend URL — read at runtime by the front-end server (lws / caddy)
# for proxying /api/* and /static/* to the real backend host.
# Client bundles do NOT read this variable; they always use relative paths.
API_BASE_URL=http://localhost:3000
API_PREFIX=/api
STATIC_PREFIX=/static
