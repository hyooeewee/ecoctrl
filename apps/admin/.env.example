# ============================================
# Backend Proxy (read at runtime by Caddy / Vite dev proxy)
# ============================================
API_BASE_URL=http://localhost:3000
API_PREFIX=/api
STATIC_PREFIX=/static
