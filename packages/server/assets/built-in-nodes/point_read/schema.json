{
  "type": "object",
  "title": "Point Read",
  "description": "Read the current value of an IoT point. Output is available to downstream nodes via {{nodeId.value}}.",
  "properties": {
    "pointName": {
      "type": "string",
      "title": "Point Name",
      "description": "The IoT point to read"
    }
  },
  "required": ["pointName"],
  "outputs": {
    "type": "object",
    "properties": {
      "value": {
        "description": "The current value of the point"
      },
      "pointName": {
        "type": "string",
        "description": "The name of the point that was read"
      },
      "values": {
        "type": "object",
        "description": "Map of all read point values"
      }
    }
  }
}
