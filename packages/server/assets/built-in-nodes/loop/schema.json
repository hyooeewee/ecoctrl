{
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "title": "模式",
      "enum": ["foreach", "while"],
      "default": "foreach"
    },
    "maxIterations": {
      "type": "number",
      "title": "最大迭代次数",
      "default": 100
    },
    "items": {
      "type": "string",
      "title": "遍历项 (foreach 模式)"
    },
    "itemVar": {
      "type": "string",
      "title": "项变量名",
      "default": "item"
    },
    "condition": {
      "type": "string",
      "title": "条件表达式 (while 模式)"
    }
  }
}
