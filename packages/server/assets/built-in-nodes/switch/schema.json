{
  "type": "object",
  "properties": {
    "expression": {
      "type": "string",
      "title": "表达式"
    },
    "cases": {
      "type": "array",
      "title": "分支条件",
      "items": {
        "type": "object",
        "properties": {
          "value": { "type": "string", "title": "匹配值" },
          "operator": {
            "type": "string",
            "title": "运算符",
            "enum": ["===", "==", "!=", "!==", ">", "<", ">=", "<="],
            "default": "==="
          }
        }
      }
    }
  },
  "required": ["expression"]
}
